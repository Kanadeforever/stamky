# stamky v0.9.3 项目说明

stamky 是一个原生 C++20 / Win32 的轻量任务栏分组启动器，以“低内存常驻、点击即出、可视化列表管理、原生 Windows 交互”为核心目标。

## 架构

进程采用单实例托盘 Host：第二实例只通过窗口 IPC 请求已有 Host 显示某个组，然后退出。Manager 与 Settings 按需创建；Runtime 菜单仍是原生 owner-draw HMENU。用户内容保存在 `data\groups.tsv`，菜单热路径读取 memory-mapped `cache\runtime.bin`，不在每次弹出时扫描源目录或重新抽取全部图标。

## v0.9.3 的定位

v0.9.3 以已确认 UI-06 为界面冻结基线，先完成正式改名，再从头审核功能代码并实际修复确认问题，然后单独补充详细简体中文源码注释，最后才进行发行整理。

本版功能审核重点覆盖：

- 文件读写与原子提交；
- UTF-8/UTF-16 严格转换；
- Model/Runtime cache 结构和计数边界；
- 图标缓存尺寸与 payload；
- Win32 `TrackPopupMenuEx` / 模态对话框嵌套消息循环；
- HWND/HMENU/HBITMAP/HICON/HFONT/COM 接口的生命周期与所有权；
- Manager 的事务一致性、拖拽状态、ListView lParam；
- Settings Cancel、格式编辑和跨重启一致性；
- Per-Monitor DPI 与失败回退；
- ShellLink、IFileOpenDialog、IContextMenu2/3；
- 语言文件与多行翻译键；
- x86/x64 manifest/CMake 架构一致性。

`runtime.bin` 已升级到 `STAMKY5 / version 5`，旧 RC2/RC2a cache 会自动失效并按当前 `groups.tsv` 重建；用户内容格式 `groups.tsv v2` 没有为了发行整理而无必要升级。

## UI-06 保持不变

本版没有借“发行整理”重新设计 UI。内容管理器的按钮排列、留白、行内重命名、无搜索框/无水平滚动，以及设置页的 Trackbar/格式编辑器布局继续以 UI-06 为准。

MSVC 主线在 UI-06 基线上只补充已确认的内容管理语义：单文件夹拖到左侧建组时保留第一层可启动文件与第一层子文件夹；“＋ 添加...”里的“添加现有分组为子组”使用原生级联 HMENU，悬停即可展开。

## 发行与验证状态

已经完成：完整源码/静态审核、功能冻结、注释只改文字的冻结点比对、默认中英文翻译覆盖检查、旧品牌兼容白名单检查、发行元数据一致性检查。

尚未在当前环境完成：Windows SDK + MSVC x86/x64 Compile/Link 与 Windows 实机运行回归。早期 v0.9.1/v0.9.2 分支的 Windows 实机构建结果不能自动等同于 v0.9.3 PASS。

因此 v0.9.3 的严谨结论是：**源码审核完成并通过当前静态验证；二进制构建与实机行为仍需要 Windows 本机或 GitHub Actions 给出最终证据。**

## Windows 范围

官方支持范围为 Windows 10+。项目本身并非概念上硬依赖 Windows 10；熟悉 Win32 的用户可自行调整 `_WIN32_WINNT`、manifest、API fallback 与工具链尝试 Windows 7/8.1，但这不属于官方支持、CI 与测试范围。

## 上游致谢

- Stacky: https://github.com/pawelt/stacky
- Stahky: https://github.com/joedf/stahky

stamky 主体源码为本项目独立实现；详细说明见 `docs\第三方与许可证说明.md`。
